---
phase: 01-foundation-architecture-brand-identity-security-baseline
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/lib/security/headers.ts
  - src/lib/security/headers.test.ts
  - next.config.ts
  - SECURITY.md
  - .github/workflows/ci.yml
autonomous: false
requirements: [SEC-06, SEC-07, SEC-09, SEC-10, SEC-12, SEC-13, SEC-14, SEC-17]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every response the app serves carries Content-Security-Policy, Strict-Transport-Security, X-Content-Type-Options, Referrer-Policy, and an anti-clickjacking header, declared in `next.config.ts` (SEC-07)"
    - "The production Content-Security-Policy contains neither of the two script-relaxation keywords; the development policy may relax script evaluation only because the dev server requires it, and an automated test proves the production and development policies differ on exactly that point (SEC-07)"
    - "The Content-Security-Policy needs no third-party host allowance, because every external destination is reached by an outbound anchor and the MVP embeds no iframe and no live feed"
    - "`SECURITY.md` states, for each of SEC-06, SEC-10, SEC-12, SEC-13, SEC-14, and SEC-17, whether it is code/config implementable in this repository or a manual account-level action, and who performs it (D-06)"
    - "`SECURITY.md` records SEC-17 as a standing constraint: adding login, a CMS, a database, a checkout, or an AI chatbot requires a fresh threat-model review before implementation"
    - "A push or pull request runs `npm ci`, the test suite, a dependency audit, and a secret scan, and a finding in any of the four fails the run (SEC-09, SEC-14)"
    - "No manual account-level control is marked satisfied inside the repository — those rows stay unchecked until the account owner performs and confirms them"
    - "No code in the repository writes to `localStorage` or `sessionStorage` (SEC-06)"
  artifacts:
    - path: "src/lib/security/headers.ts"
      provides: "buildCsp(mode) and securityHeaders(mode) — the single source of the header set, extracted so it is unit-testable"
      exports: ["buildCsp", "securityHeaders"]
    - path: "src/lib/security/headers.test.ts"
      provides: "SEC-07 automated assertions over the production and development policies"
    - path: "next.config.ts"
      provides: "headers() returning securityHeaders() for every route"
      contains: "headers"
    - path: "SECURITY.md"
      provides: "D-06 security baseline checklist, split into code/config items and manual account actions, plus the SEC-17 standing constraint"
      min_lines: 70
    - path: ".github/workflows/ci.yml"
      provides: "npm ci + test + audit + secret-scan gate on push and pull_request"
      contains: "npm ci"
  key_links:
    - from: "next.config.ts"
      to: "src/lib/security/headers.ts"
      via: "imports securityHeaders() and returns it from the async headers() hook"
      pattern: "securityHeaders"
    - from: "src/lib/security/headers.test.ts"
      to: "src/lib/security/headers.ts"
      via: "asserts the production policy omits the script-relaxation keywords the development policy carries"
      pattern: "buildCsp"
    - from: ".github/workflows/ci.yml"
      to: "package-lock.json"
      via: "npm ci installs strictly from the committed lockfile, never resolving fresh versions"
      pattern: "npm ci"
  prohibitions:
    - "MUST NOT state or imply anywhere in `SECURITY.md`, the CI output, or a commit message that this site is fully secure, hardened, or that a clean scanner run proves the absence of vulnerabilities. PROJECT.md forbids the claim explicitly: a clean scan is partial evidence, never proof, and an overconfident security document is worse than none because it stops the next reader from looking (P8, transparency)."
    - "MUST NOT mark any manual account-level control — MFA, branch protection, required PR review, secret scanning, registrar lock — as satisfied from inside this repository. Only the account owner can perform them, and a pre-ticked checklist converts an unprotected `main` branch into a documented false belief (P9, transparency/integrity)."
  canon_breadcrumbs:
    - "OWASP Top Ten application classes (injection, broken access control, SSRF) are canon — covered by `/gsd-secure-phase`; not minted as bespoke prohibitions here, and most do not apply to a static site with no backend."
    - "Secret detection in commit history is canon — covered by the secret-scan CI step wired in this plan plus `/gsd-secure-phase`; not minted as a bespoke prohibition."
    - "GDPR / consent / data-retention is canon and out of scope — this MVP collects no personal data and ships no analytics; breadcrumb to `/gsd-secure-phase` if a later phase adds either."
  flagged_assumptions:
    - "SEC-06 (edge row: unclassified, unresolved) — N/A as a data-shape edge. SEC-06 forbids putting sensitive values in browser storage. This phase writes no client-side storage code at all, so there is no shape to edge-test; verified as an absence assertion over the source tree plus a standing code-review rule in SECURITY.md. Carried as an explicit assumption, not dropped."
    - "SEC-07 (edge row: unclassified, unresolved) — N/A as a data-shape edge. SEC-07 is a configuration requirement. Verified by unit assertions over the emitted header set and policy string; production verification on the live domain is Phase 5 (SEC-11) by design. Carried as an explicit assumption, not dropped."
    - "SEC-10 (edge row: unclassified, unresolved) — N/A as a data-shape edge. SEC-10 is a manual account action (MFA on three accounts). Cannot be verified from inside the repository; recorded as a manual checklist row with a named owner per D-06. Carried as an explicit assumption, not dropped."
    - "SEC-12 (edge row: concurrency, unresolved) — the probe's `concurrency` hit is a word-overlap false positive, matching 'merge' in 'PR obrigatório antes de qualquer merge'. Branch protection is a repository setting, not a concurrent operation with an interruption guarantee. Classified N/A and recorded as a manual checklist row. Carried as an explicit assumption, not dropped."
    - "SEC-13 (edge row: adjacency, unresolved) — word-overlap false positive on the same 'merge' token. Required PR review has no notion of two values 'touching or being equal'. Classified N/A. Carried as an explicit assumption, not dropped."
    - "SEC-13 (edge row: empty, unresolved) — the probe asks about empty/single-element input. There is one arguably real reading: what happens when a PR has zero reviewers available, i.e. a solo-developer repository where the sole maintainer is also the only possible approver. That is a genuine operational question and is answered in SECURITY.md rather than silently ignored: the checklist states explicitly that a solo maintainer must decide between self-approval-permitted branch protection and adding a second reviewer, and names it as an open operational choice. Carried as an explicit assumption with an added acceptance criterion."
    - "SEC-13 (edge row: ordering, unresolved) — word-overlap false positive; 'stable output order for equal elements' has no meaning for a branch-protection policy. Classified N/A. Carried as an explicit assumption, not dropped."
    - "SEC-14 (edge row: unclassified, unresolved) — N/A as a data-shape edge. SEC-14 requires automated secret scanning to be active. Verified in-repository by the CI secret-scan step; the GitHub-native half depends on the repository-visibility decision gated in Task 2. Carried as an explicit assumption, not dropped."
    - "SEC-17 (edge row: unclassified, unresolved) — N/A as a data-shape edge. SEC-17 is a process rule about future features, not a runtime behaviour. Verified by its presence as a named standing constraint in SECURITY.md. Carried as an explicit assumption, not dropped."
---

<objective>
Implement the half of the security baseline that is code and configuration — the response header
set and its Content-Security-Policy, and a CI gate that installs from the lockfile, runs the tests,
audits dependencies, and scans for secrets — and document the other half honestly as manual
account-level actions that only the account owner can perform.

Purpose: SEC-07 and SEC-09 are implementable here and now. SEC-10/12/13/14 are not: they are
dashboard actions on GitHub, Vercel, and a domain registrar, and one of them is blocked on a
decision the client has not made (repository visibility). Pretending otherwise would produce a
ticked checklist over an unprotected branch.

Output: a header set active on every route, a CI workflow that fails on a real finding, and a
`SECURITY.md` that is accurate about who still has to do what.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-PATTERNS.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-CONTEXT.md
@ask_questions/QA-LOG.md
</context>

## Artifacts this phase produces

New symbols created by plan 01-04 (none exist before this plan):

| Symbol / path | Kind | Signature or shape |
|---|---|---|
| `buildCsp` | function | `(mode: "development" \| "production") => string` — `src/lib/security/headers.ts` |
| `securityHeaders` | function | `(mode: "development" \| "production") => { key: string; value: string }[]` |
| `src/lib/security/headers.test.ts` | file | Vitest guard over the policy and header set |
| `SECURITY.md` | file | D-06 checklist, repository root |
| `.github/workflows/ci.yml` | file | CI gate: `npm ci`, test, audit, secret scan |
| `next.config.ts` `headers()` | config hook | extended from the plan 01-01 scaffold shell |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract a testable security-header module and wire it into next.config.ts</name>
  <files>src/lib/security/headers.ts, src/lib/security/headers.test.ts, next.config.ts</files>
  <read_first>
    - `next.config.ts` — the scaffold shell written by plan 01-01; read its current contents before editing rather than assuming the template
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-PATTERNS.md` — the `next.config.ts` template under "`next.config.ts` (config, request-response headers)", which reproduces Next.js's own non-nonce CSP example verbatim
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-RESEARCH.md` — `### Pattern 3` (why no third-party host allowance is needed) and the `### Alternatives Considered` row rejecting a nonce-based policy
    - `.planning/REQUIREMENTS.md` — SEC-07, SEC-08, SEC-11 (note that SEC-08 and SEC-11 are Phase 5 scope; this task only has to make the configuration correct)
    - `vitest.config.mts` — the runner config from plan 01-01
  </read_first>
  <behavior>
    - `buildCsp("production")` contains the directives `default-src 'self'`, `script-src 'self'`, `style-src`, `img-src`, `font-src`, `object-src 'none'`, `base-uri 'self'`, `form-action 'self'`, `frame-ancestors 'none'`, and `upgrade-insecure-requests`.
    - `buildCsp("production")` does NOT contain either script-relaxation keyword (the eval keyword or the inline keyword) inside its `script-src` directive.
    - `buildCsp("development")` DOES contain the eval keyword in its `script-src` directive, and is otherwise byte-identical to the production policy.
    - `buildCsp` output contains no newline characters (a multi-line header value is rejected by HTTP).
    - `buildCsp` output names no third-party hostname — no iFood, WhatsApp, Instagram, or Google host appears in any directive, because the MVP embeds nothing.
    - `securityHeaders("production")` returns entries whose keys include `Content-Security-Policy`, `Strict-Transport-Security`, `X-Content-Type-Options`, `Referrer-Policy`, and `X-Frame-Options`.
    - The `Strict-Transport-Security` value includes a max-age of at least one year, `includeSubDomains`, and `preload`.
    - The `X-Content-Type-Options` value is exactly `nosniff` and the `Referrer-Policy` value is exactly `strict-origin-when-cross-origin`.
    - `securityHeaders` returns no duplicate keys.
  </behavior>
  <action>
    Write `src/lib/security/headers.test.ts` first, watch it fail against the missing module, then
    implement.

    Create `src/lib/security/headers.ts` exporting two functions. `buildCsp(mode)` returns the
    policy as a single-line string; `securityHeaders(mode)` returns the array of `{ key, value }`
    entries Next.js expects. Extracting them out of `next.config.ts` is the point of this task:
    a policy defined inline in the config file cannot be unit-tested, and SEC-07 is the requirement
    most likely to be silently wrong.

    Build the policy from the PATTERNS.md template, which reproduces the framework's own non-nonce
    example. The directive set is `default-src`, `script-src`, `style-src`, `img-src`, `font-src`,
    `object-src`, `base-uri`, `form-action`, `frame-ancestors`, and `upgrade-insecure-requests`.
    `img-src` additionally permits the `blob:` and `data:` schemes, which the framework's image
    optimizer requires. `style-src` permits inline styles, which the framework's own style
    injection requires.

    The `mode` parameter exists for exactly one reason: the development server needs script
    evaluation permitted, and production must not have it. Branch on `mode` only inside the
    `script-src` directive, and derive the mode in `next.config.ts` from `process.env.NODE_ENV`.
    Strip all newlines from the assembled string before returning it.

    Do NOT add any third-party hostname to any directive. RESEARCH.md `### Pattern 3` establishes
    why: LOCAL-01 rules out an embedded map, MARCA-05's note rules out a live Instagram feed, and
    every remaining external interaction is a plain outbound anchor, which no directive governs.
    Adding hosts speculatively widens the policy for no benefit.

    Do NOT switch to a nonce-based policy. RESEARCH.md rejects it for this project: nonces force
    every route into per-request dynamic rendering, which kills static generation for a site with
    no per-user state, and a documented framework issue lets a re-render inherit another
    requester's header on some hosts.

    Then edit `next.config.ts` to import `securityHeaders` from `./src/lib/security/headers` and
    return it from the async `headers()` hook for the route pattern matching every path. Use a
    relative import rather than the `@/` alias — the config file is loaded outside the app's module
    graph, where the alias is not guaranteed to resolve.
  </action>
  <verify>
    <automated>npx vitest run src/lib/security/headers.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests collected, or any assertion is reported as failed</fails_when>
    <automated>npm --prefix . run build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile" or "Invalid next.config.ts"</fails_when>
    <automated>node -e "const fs=require('fs');const cfg=fs.readFileSync('next.config.ts','utf8');if(!/securityHeaders/.test(cfg)){console.error('next.config.ts does not import the header module');process.exit(1)};if(!/headers\s*\(/.test(cfg)){console.error('next.config.ts declares no headers() hook');process.exit(1)};const mod=fs.readFileSync('src/lib/security/headers.ts','utf8');const hosts=['ifood','whatsapp','wa.me','instagram','googleapis','google.com'];const leaked=hosts.filter(h=>mod.includes(h));if(leaked.length){console.error('policy names third-party hosts it does not need: '+leaked.join(','));process.exit(1)};console.log('header wiring OK')"</automated>
    <fails_when>non-zero exit, or stderr reports a missing import, a missing headers hook, or a speculative third-party host in the policy</fails_when>
    <human-check>
      Start the dev server and load the home page. Confirm the browser console shows no
      Content-Security-Policy violation for the framework's own scripts, styles, or fonts — a
      policy that blocks the app's own assets is a policy that will be quietly loosened later.
      Full verification against the real production domain is Phase 5 (SEC-11), not this task.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/lib/security/headers.ts` exports `buildCsp` and `securityHeaders`.
    - `buildCsp("production")` output contains all ten required directives and no newline character.
    - `buildCsp("production")` output's `script-src` directive contains neither script-relaxation
      keyword; `buildCsp("development")`'s does contain the eval keyword, and the two strings differ
      only in that directive.
    - `buildCsp` output contains no third-party hostname.
    - `securityHeaders("production")` returns entries keyed `Content-Security-Policy`,
      `Strict-Transport-Security`, `X-Content-Type-Options`, `Referrer-Policy`, and
      `X-Frame-Options`, with no duplicate keys.
    - `Strict-Transport-Security` includes a max-age of at least 31536000, `includeSubDomains`, and
      `preload`.
    - `next.config.ts` imports `securityHeaders` by a relative path and returns it from `headers()`
      for a source pattern matching every route.
    - `src/lib/security/headers.ts` contains no nonce generation and no call into `crypto`.
    - `npx vitest run src/lib/security/headers.test.ts` exits 0 with at least 10 passing assertions.
  </acceptance_criteria>
  <done>Every route's response header set is defined in one testable module, the production policy is proven to differ from the development policy on exactly the script-evaluation point, and the build succeeds.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Decide GitHub repository visibility before the SEC-12/13/14 checklist is written</name>
  <decision>Will the It's Garlic repository be public, private on a paid GitHub plan, or private on GitHub Free?</decision>
  <context>
    No git remote exists yet — `git remote -v` returns nothing. The answer determines which of
    SEC-12, SEC-13, and SEC-14 can actually be satisfied, and therefore what Task 3 is allowed to
    write into `SECURITY.md`.

    RESEARCH.md's single most consequential finding (`### Pitfall D`, `## Open Questions` item 1):
    classic branch-protection rules on a **private** repository are unavailable on GitHub Free for
    a personal account — the "require a pull request before merging" setting is simply not exposed.
    They are available on a public repository for free, or on a private repository from GitHub Pro
    upward. The same split governs secret scanning: free on public repositories, paid on private
    ones. Confidence on the plan-tier detail is MEDIUM (fetched from official docs, not confirmed
    by logging into a real account), and pricing pages change without a changelog, so re-check it
    at execution time.

    This is close to a one-way door. Publishing a repository cannot be undone by flipping it back
    to private: anything pushed while public may already have been cloned, forked, or archived by a
    third party. SEC-04 means nothing sensitive should be in the repository in the first place,
    which makes "public" defensible here — but it is the client's call, not the agent's.
  </context>
  <options>
    <option id="public">
      <name>Public repository on GitHub Free</name>
      <pros>Branch protection, required pull-request review, and native secret scanning with push protection are all available at no cost, so SEC-12, SEC-13, and SEC-14 are fully satisfiable. No recurring spend. SEC-04 already guarantees the repository holds no secrets, so there is nothing to expose.</pros>
      <cons>Effectively irreversible — the source, the commit history, and the `.planning/` decision record become permanently public and may be mirrored by third parties. Client business context in `PROJECT.md` and `QA-LOG.md` becomes readable by anyone, including competitors.</cons>
    </option>
    <option id="private-paid">
      <name>Private repository on GitHub Pro (or Team)</name>
      <pros>SEC-12, SEC-13, and SEC-14 are all satisfiable with GitHub-native features while the source and the client's business context stay private. Fully reversible — the plan can be downgraded later.</pros>
      <cons>A recurring subscription cost (roughly US$4/month for Pro at the time RESEARCH.md was written; re-verify). Secret scanning on private repositories may require GitHub Advanced Security depending on the tier, which costs more than Pro alone.</cons>
    </option>
    <option id="private-free">
      <name>Private repository on GitHub Free, accepting the gap</name>
      <pros>No cost and the source stays private.</pros>
      <cons>SEC-12 and SEC-13 cannot be enforced by the platform — `main` stays pushable directly and no review can be required, so the compromised-account threat (T-01-03) keeps its easiest path. SEC-14's GitHub-native half is unavailable; only the CI secret-scan step from Task 3 remains, which catches a secret at pull-request time rather than blocking the push. Task 3 would have to record SEC-12 and SEC-13 as accepted gaps with the client's explicit sign-off, not as satisfied.</cons>
    </option>
  </options>
  <resume-signal>Select: public, private-paid, or private-free</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write the D-06 security baseline checklist and the CI gate</name>
  <reversibility rating="one-way">The repository-visibility choice resolved in Task 2 cannot be undone once the repository is published; Task 3 records the chosen path and its consequences for SEC-12/13/14 rather than re-deciding it.</reversibility>
  <precondition>The Task 2 decision is resolved and its chosen option id is known (public, private-paid, or private-free)</precondition>
  <files>SECURITY.md, .github/workflows/ci.yml</files>
  <read_first>
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-RESEARCH.md` — `## Security Domain` in full: the ASVS applicability table, the known-threat/STRIDE table, and the "Security Baseline Checklist (D-06 — code vs manual split)" whose two lists this task turns into `SECURITY.md`
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-CONTEXT.md` — decision D-06, which is the authority for documenting rather than executing the manual half
    - `.planning/REQUIREMENTS.md` — SEC-01 through SEC-17 in full, plus bloco 4b (the four operational verifications)
    - `ask_questions/QA-LOG.md` — the threat-model rationale behind the SEC requirements
    - `.planning/PROJECT.md` — `## Constraints`, which forbids any claim of total security
    - `package.json` — the `test` script name and the `engines.node` pin from plan 01-01, both of which the CI workflow must match
  </read_first>
  <action>
    Create `SECURITY.md` at the repository root. RESEARCH.md's Claude's-Discretion note leaves the
    format open; a dedicated root file is chosen over a README section because GitHub surfaces it
    in the repository's security tab, which puts it in front of the person who has to act on it.

    Structure it in four parts.

    **Part 1 — Escopo e limites.** State plainly what this document is: a set of verifiable
    criteria, not a guarantee. Reproduce PROJECT.md's own rule in the client's words: there is no
    claim of total security, and a clean scanner run is partial evidence rather than proof. State
    that the MVP has no backend, no authentication, no database, and no payment processing, so the
    server-side classes of attack (injection into a query, authentication bypass, remote code
    execution) are out of scope by construction rather than by mitigation. Name the assets that do
    matter, per QA-LOG.md: the integrity of published content, and above all the integrity of the
    four outbound destinations.

    **Part 2 — Itens de código/configuração (implementáveis neste repositório).** One row per item,
    each naming the file that implements it and the command that verifies it. Cover: the response
    header set and its policy (`src/lib/security/headers.ts`, verified by
    `src/lib/security/headers.test.ts`); the dotenv exclusions (`.gitignore`, verified by
    `git check-ignore`); the outbound-destination allowlist (`src/lib/integrations/allowlist.ts`,
    verified by `src/lib/integrations/allowlist.test.ts`); data validation at the repository
    boundary (`src/lib/schemas/*`); the committed lockfile and the `npm ci` install path; the
    dependency audit step; and the secret-scan step. For SEC-06, record it as a standing
    code-review rule with its current status: no code in this repository writes to `localStorage`
    or `sessionStorage`, and any future client-side storage requires review before merge.

    **Part 3 — Ações manuais do dono das contas (fora do repositório).** Per D-06, these are
    documented and left unchecked. Every row states the requirement id, the exact place the action
    is performed, who performs it (the account owner, not the agent), and how it is confirmed. Cover
    SEC-10 (multi-factor authentication on GitHub, on Vercel, and on the domain registrar — noting
    that the registrar is not yet chosen, per PROJECT.md's pendências), SEC-12 and SEC-13 (branch
    protection on `main` and required review before any merge that triggers a production deploy),
    SEC-14 (secret scanning with push protection), and SEC-15 (registrar lock, which ROADMAP.md
    assigns to Phase 5 and which is listed here only for traceability).

    Write the SEC-12/13/14 rows according to the option the Task 2 decision selected. If public or
    private-paid: state that the GitHub-native controls are available and list the exact settings
    path to enable them. If private-free: state explicitly that SEC-12 and SEC-13 cannot be
    enforced by the platform on this tier, record them as accepted gaps requiring the client's
    written sign-off, and state that the CI secret-scan step is the only remaining SEC-14 control.

    Also answer, in the SEC-13 row, the solo-maintainer question the requirements leave open: when
    the only maintainer is also the only possible approver, required-review is either satisfied by
    permitting self-approval on a protected branch (which preserves the no-direct-push guarantee but
    not the second-pair-of-eyes guarantee) or by adding a second reviewer. State that this is an
    open operational choice for the account owner and name both consequences. Do not silently pick
    one.

    **Part 4 — SEC-17, restrição permanente.** State the standing rule in its own section, worded so
    a future reader cannot miss it: before adding login, a CMS, a database, a checkout, or an
    AI chatbot, a fresh threat-model review must be performed and the security requirements updated
    **before** implementation begins. List the five triggering features by name. Note that the
    WhatsApp AI ordering bot already recorded as a v2 idea is one of them.

    Every row in Parts 2 and 3 uses an unchecked checkbox. Do not tick a single box in Part 3 — the
    agent cannot perform those actions, and a ticked box would assert something false.

    Then create `.github/workflows/ci.yml`. Trigger on `push` and on `pull_request`. Use a single
    job on `ubuntu-latest` with these steps, in order: check out the repository with full history
    (the secret scan needs it to inspect commits, not just the working tree); set up Node using the
    same major version family the `engines.node` pin in `package.json` allows; run `npm ci` — never
    `npm install`, because `ci` installs strictly from the committed lockfile and fails on drift
    instead of silently resolving a different tree; run the lint script; run the test script; run a
    dependency audit gated at the high severity level, so a high or critical advisory fails the job
    while a low one does not block; and run a secret scan using the `gitleaks` static binary
    invoked directly against the repository, which RESEARCH.md prefers over the marketplace action
    to sidestep the private-repository licensing ambiguity entirely.

    Do not add `continue-on-error` to any step and do not append a failure-swallowing fallback to
    any command. A gate that cannot fail measures nothing — that is the whole reason this workflow
    exists rather than a note asking someone to remember to audit.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('SECURITY.md','utf8');const lines=t.split('\n').length;if(lines<70){console.error('SECURITY.md is only '+lines+' lines; the D-06 split cannot be complete');process.exit(1)};const ids=['SEC-06','SEC-07','SEC-09','SEC-10','SEC-12','SEC-13','SEC-14','SEC-17'];const missing=ids.filter(i=>!t.includes(i));if(missing.length){console.error('SECURITY.md does not address: '+missing.join(','));process.exit(1)};console.log('SECURITY.md covers all 8 requirement ids across '+lines+' lines')"</automated>
    <fails_when>non-zero exit, or stderr reports a line count under 70 or names any unaddressed requirement id</fails_when>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('SECURITY.md','utf8');const manual=t.split(/^##\s/m).find(s=>/^A[cç][oõ]es manuais/i.test(s));if(!manual){console.error('SECURITY.md has no manual-actions section');process.exit(1)};const ticked=(manual.match(/- \[x\]/gi)||[]).length;if(ticked>0){console.error(ticked+' manual account-level control(s) are pre-ticked inside the repository; only the account owner may confirm them');process.exit(1)};const unticked=(manual.match(/- \[ \]/g)||[]).length;if(unticked<4){console.error('manual-actions section lists only '+unticked+' unchecked rows; SEC-10/12/13/14 all require one');process.exit(1)};console.log('manual section OK: '+unticked+' unchecked rows, 0 pre-ticked')"</automated>
    <fails_when>non-zero exit, or stderr reports a missing manual-actions section, any pre-ticked manual row, or fewer than 4 unchecked rows</fails_when>
    <automated>node -e "const fs=require('fs');const w=fs.readFileSync('.github/workflows/ci.yml','utf8');const need=['npm ci','pull_request','audit','gitleaks'];const missing=need.filter(s=>!w.includes(s));if(missing.length){console.error('ci.yml is missing required step or trigger: '+missing.join(','));process.exit(1)};if(/continue-on-error/.test(w)){console.error('ci.yml contains continue-on-error, which converts a gate into a notification');process.exit(1)};if(/npm install/.test(w)){console.error('ci.yml uses npm install instead of npm ci, so it does not install from the committed lockfile');process.exit(1)};console.log('ci gate OK')"</automated>
    <fails_when>non-zero exit, or stderr reports a missing trigger or step, a `continue-on-error` escape hatch, or a non-lockfile install</fails_when>
    <automated>node -e "const{spawnSync}=require('child_process');const r=spawnSync('git',['grep','-l','-e','localStorage','-e','sessionStorage','--','src'],{encoding:'utf8'});if(r.error){console.error('git grep could not run: '+r.error.message);process.exit(1)};if(r.status===0){console.error('SEC-06: browser storage used in: '+r.stdout.trim().split('\n').join(', '));process.exit(1)};if(r.status!==1){console.error('git grep exited unexpectedly with status '+r.status+': '+r.stderr.trim());process.exit(1)};console.log('SEC-06 OK: no browser storage use under src')"</automated>
    <fails_when>non-zero exit, or stderr contains "SEC-06: browser storage used in", "git grep could not run", or "git grep exited unexpectedly" — a clean pass prints "SEC-06 OK" and only git-grep status 1 (no match) is treated as clean</fails_when>
    <automated>npm --prefix . run build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile"</fails_when>
    <human-check>
      Open the repository's Actions tab after the first push and confirm the workflow ran and that
      every step is green. Then confirm the audit and secret-scan steps can actually fail: check
      their logs show a real scan result rather than a skipped step. A CI gate nobody has seen go
      red is an assumption, not a control.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `SECURITY.md` exists at the repository root, is at least 70 lines, and mentions each of
      SEC-06, SEC-07, SEC-09, SEC-10, SEC-12, SEC-13, SEC-14, and SEC-17 by id.
    - `SECURITY.md` has four top-level sections matching the scope/code/manual/SEC-17 structure.
    - Every row in the manual-actions section is an unchecked checkbox; the section contains zero
      checked boxes and at least four unchecked rows.
    - The SEC-12/13/14 rows reflect the option selected in Task 2, and if `private-free` was
      selected they are recorded as accepted gaps requiring written client sign-off rather than as
      satisfiable.
    - The SEC-13 row names both solo-maintainer options (self-approval on a protected branch versus
      adding a second reviewer) and their differing consequences, without picking one.
    - The SEC-17 section names all five triggering features: login, CMS, database, checkout, and
      AI chatbot.
    - `SECURITY.md` contains no claim that the site is fully secure, hardened, or free of
      vulnerabilities, and states that a clean scan is partial evidence rather than proof.
    - `.github/workflows/ci.yml` triggers on both `push` and `pull_request`, runs `npm ci`, the lint
      script, the test script, a high-severity-gated audit, and a `gitleaks` scan.
    - `.github/workflows/ci.yml` contains no `continue-on-error` and no `npm install`.
    - `git grep -l -e localStorage -e sessionStorage -- src` returns no match.
  </acceptance_criteria>
  <done>`SECURITY.md` accurately splits implemented controls from pending manual actions with nothing falsely ticked, and the CI workflow fails the build on a lockfile drift, a failing test, a high-severity advisory, or a detected secret.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser ← CDN / edge response | Response headers are the only control the app has over how a browser treats its own content; a missing policy is exploitable by a network attacker or a framing site. |
| contributor → git history → production deploy | A push to the default branch triggers a deploy. Whoever controls a credential on that path controls what visitors see. |
| npm registry → CI runner → build artefact | A dependency resolved at CI time, rather than from the lockfile, is a dependency nobody reviewed. |
| GitHub / Vercel / registrar dashboards | Account-level controls live entirely outside this repository and cannot be asserted from within it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering / Information Disclosure | response headers, `src/lib/security/headers.ts` | high | mitigate | Ten-directive policy plus HSTS, nosniff, Referrer-Policy, and an anti-clickjacking header returned for every route; production policy proven by unit test to omit both script-relaxation keywords. Production-domain verification is Phase 5 (SEC-11). |
| T-01-03 | Tampering / Elevation of Privilege | GitHub / Vercel / registrar accounts | high | transfer | Cannot be mitigated in code. Documented as named, unchecked manual rows in `SECURITY.md` Part 3 with the exact settings path and the responsible owner (D-06). If Task 2 selects `private-free`, SEC-12 and SEC-13 are recorded as accepted gaps requiring the client's written sign-off — never as satisfied. |
| T-01-SC | Tampering | CI dependency install, `.github/workflows/ci.yml` | high | mitigate | `npm ci` installs strictly from the committed lockfile and fails on drift; a high-severity-gated audit step runs on every push and pull request; no `continue-on-error` anywhere in the workflow (SEC-09). |
| T-01-02 | Information Disclosure | commit history, CI secret scan | high | mitigate | `gitleaks` static binary run against full checkout history on every push and pull request; complements the plan 01-01 `.gitignore` baseline. The GitHub-native push-protection half depends on the Task 2 visibility decision (SEC-14). |
| T-01-15 | Information Disclosure | browser storage | low | mitigate | No code in this repository touches `localStorage` or `sessionStorage`; asserted by a source-tree check in Task 3 and recorded as a standing code-review rule (SEC-06). |
| T-01-16 | Spoofing | clickjacking / framing of the site | medium | mitigate | `frame-ancestors 'none'` in the policy plus the legacy `X-Frame-Options` header for older browsers. |
| T-01-17 | Repudiation | production deploy provenance and rollback | low | accept | Deployment provenance, tested rollback (SEC-16), and registrar lock (SEC-15) are assigned to Phase 5 by ROADMAP.md; listed in `SECURITY.md` for traceability only, not implemented here. |

## ASVS applicability (level 1)

| ASVS Category | Applies | Control in this plan |
|---|---|---|
| V2 Authentication | No | No accounts or login in the MVP. |
| V3 Session Management | No | No sessions. |
| V4 Access Control | No (in-app) | The access control that matters is repository/account-level — Part 3 of `SECURITY.md`. |
| V5 Input Validation | Yes | Zod at the repository boundary (plan 01-02); untrusted search/URL input arrives in Phase 3 (SEC-01). |
| V6 Cryptography | No | No secrets to sign or store. |
| V14 Configuration | Yes | `src/lib/security/headers.ts`, `.gitignore`, `server-only`, `.github/workflows/ci.yml`. |
</threat_model>

<verification>
1. `npx vitest run src/lib/security/headers.test.ts` exits 0 with at least 10 assertions.
2. `npm run build` exits 0 with the `headers()` hook wired.
3. `SECURITY.md` is at least 70 lines, names all eight requirement ids, and has zero pre-ticked manual rows.
4. `.github/workflows/ci.yml` runs `npm ci`, lint, test, audit, and `gitleaks`, with no `continue-on-error`.
5. `git grep -l -e localStorage -e sessionStorage -- src` returns no match.
</verification>

<success_criteria>
Every control this repository can enforce is enforced and tested; every control it cannot enforce
is named, unchecked, assigned to the account owner, and honest about whether the chosen GitHub plan
can satisfy it at all.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-04-SUMMARY.md` when done.
Record the option id selected in Task 2 and its consequence for SEC-12/13/14, plus the exact list
of manual rows left unchecked, so `/gsd-verify-work` and the Phase 5 security pass inherit an
accurate picture rather than re-deriving it.
</output>
