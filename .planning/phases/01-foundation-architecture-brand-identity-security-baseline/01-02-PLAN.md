---
phase: 01-foundation-architecture-brand-identity-security-baseline
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/data/store.ts
  - src/data/links.ts
  - src/lib/schemas/store.schema.ts
  - src/lib/schemas/link.schema.ts
  - src/lib/schemas/store.schema.test.ts
  - src/lib/repositories/store-repository.ts
  - src/lib/integrations/allowlist.ts
  - src/lib/integrations/ifood.ts
  - src/lib/integrations/whatsapp.ts
  - src/lib/integrations/instagram.ts
  - src/lib/integrations/maps.ts
  - src/lib/integrations/allowlist.test.ts
  - src/lib/integrations/integrations.test.ts
  - src/app/page.tsx
  - eslint.config.mjs
autonomous: true
requirements: [ARQ-02, ARQ-03, INTEGRA-04, SEC-03, CONT-03]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Loading the site's home page shows the real store name, street address, and neighbourhood, read from `src/data/store.ts` through `getStoreInfo()` — not typed into the component"
    - "The home page shows a working 'Pedir no iFood' anchor whose href was built by `buildIFoodUrl()` and passed `assertAllowedHost()`"
    - "Editing `src/data/store.ts` to remove a required field makes `npm run build` fail instead of shipping a page with a blank address (ARQ-03)"
    - "Every one of the four outbound destinations (iFood, WhatsApp, Instagram, Maps) is produced by a function in `src/lib/integrations/` that calls `assertAllowedHost()` before returning"
    - "A URL whose hostname is a look-alike of an official destination is rejected by `assertAllowedHost()` with a thrown error, proven by an automated test (SEC-03)"
    - "Every unconfirmed external destination in `src/data/links.ts` carries `confirmed: false` and a non-empty `pendingConfirmation` note, and its URL path contains an obviously-invalid placeholder token rather than a plausible guess (CONT-03)"
    - "A React component outside `src/lib/repositories/` and `src/lib/integrations/` that imports from `@/data/*` fails `npm run lint` (ARQ-02)"
    - "Store opening hours parse only while `provisional` is the literal `true`; flipping it to confirmed requires a deliberate schema edit, not a silent data change"
  artifacts:
    - path: "src/lib/schemas/store.schema.ts"
      provides: "Zod v4 schema + inferred StoreInfo type for the store/address/modalities/hours shape"
      exports: ["storeInfoSchema", "StoreInfo"]
    - path: "src/lib/schemas/link.schema.ts"
      provides: "Zod v4 schema + inferred type for an external destination, including the confirmed/pendingConfirmation invariant"
      exports: ["externalLinkSchema", "externalLinksSchema", "ExternalLink"]
    - path: "src/lib/repositories/store-repository.ts"
      provides: "getStoreInfo() — the only sanctioned path from store data to UI (ARQ-02 seam 1)"
      exports: ["getStoreInfo"]
    - path: "src/lib/integrations/allowlist.ts"
      provides: "ALLOWED_HOSTS + assertAllowedHost() — the single outbound-destination guard (SEC-03)"
      exports: ["ALLOWED_HOSTS", "assertAllowedHost"]
    - path: "src/lib/integrations/ifood.ts"
      provides: "buildIFoodUrl() — allowlisted iFood store URL (ARQ-02 seam 2)"
      exports: ["buildIFoodUrl"]
    - path: "src/data/store.ts"
      provides: "Hand-authored store facts sourced from PROJECT.md; hours flagged provisional"
    - path: "src/data/links.ts"
      provides: "Hand-authored external destinations; iFood and WhatsApp carry placeholder paths pending client confirmation"
    - path: "src/app/page.tsx"
      provides: "Home route rendering repository-sourced store data and an allowlisted outbound CTA"
    - path: "src/lib/integrations/allowlist.test.ts"
      provides: "SEC-03 automated destination validation — accepts official hosts, rejects look-alikes"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/lib/repositories/store-repository.ts"
      via: "calls getStoreInfo() to obtain store facts; never imports @/data directly"
      pattern: "getStoreInfo"
    - from: "src/lib/repositories/store-repository.ts"
      to: "src/lib/schemas/store.schema.ts"
      via: "storeInfoSchema.parse(rawStore) — the build-time validation gate"
      pattern: "storeInfoSchema\\.parse"
    - from: "src/app/page.tsx"
      to: "src/lib/integrations/ifood.ts"
      via: "calls buildIFoodUrl() for the CTA href; no URL literal in the component"
      pattern: "buildIFoodUrl"
    - from: "src/lib/integrations/ifood.ts"
      to: "src/lib/integrations/allowlist.ts"
      via: "assertAllowedHost(url) before returning — same for whatsapp/instagram/maps"
      pattern: "assertAllowedHost"
  prohibitions:
    - "MUST NOT allow any customer-facing order or contact link to resolve to a destination outside the official allowlist — including via a link shortener, a redirect, or a hostname that merely ends with an allowed domain. Sending a hungry visitor to a fraudulent ordering page is the top risk this project's own threat model names (P1, safety)."
    - "MUST NOT render an unconfirmed placeholder destination as an ordinary working link. A placeholder must stay obviously invalid on click and machine-distinguishable in the data layer (`confirmed: false`), never a plausible-looking guessed phone number or store slug that could silently ship to production (P2, transparency)."
    - "MUST NOT silently default, coerce, or drop a malformed commercial field (price, address, hours). Invalid data must fail the build loudly — a page that renders a confident-looking wrong price is worse than a page that does not build (P3, values/trust)."
  canon_breadcrumbs:
    - "Reflected XSS via search/URL parameters, injection, and path traversal are canon — covered by `/gsd-secure-phase` and by SEC-01/SEC-02 in Phases 3 and 5; not minted as bespoke prohibitions here."
    - "Prototype pollution through `new URL()` parsing is canon — covered by `/gsd-secure-phase` and eslint security plugins; not minted here."
  flagged_assumptions:
    - "ARQ-02 (edge row: unclassified, unresolved) — N/A as a data-shape edge. ARQ-02 is a structural separation requirement. Verified by the ESLint `no-restricted-imports` rule plus the `getStoreInfo`/`assertAllowedHost` call-path assertions, not by an input-shape edge case. Carried as an explicit assumption, not dropped."
    - "ARQ-03 (edge row: unclassified, unresolved) — the probe did not classify it, but this requirement DOES have a genuine empty/missing-field edge, and it is covered explicitly: `store.schema.test.ts` asserts that a missing required field and an empty required string are both rejected, and that an empty `schedule` array is accepted while `provisional` remains true. Carried as an explicit assumption with an added acceptance criterion."
    - "INTEGRA-04 (edge row: unclassified, unresolved) — N/A as a data-shape edge. INTEGRA-04 is a centralisation requirement. Verified by asserting every integration module calls `assertAllowedHost` and by the lint rule barring direct `@/data/*` imports from components. Carried as an explicit assumption, not dropped."
    - "SEC-03 (edge row: unclassified, unresolved) — the probe did not classify it, but an adjacency edge genuinely applies and is covered: `allowlist.test.ts` asserts that a hostname which merely *contains* or *suffixes* an allowed domain is rejected, so 'just touching' an allowed value does not merge into acceptance. Carried as an explicit assumption with an added acceptance criterion."
    - "CONT-03 (edge row: unclassified, unresolved) — N/A as a data-shape edge. CONT-03 is a content-governance requirement. Verified by the `confirmed`/`pendingConfirmation` schema invariant and the placeholder-token assertion. Carried as an explicit assumption, not dropped."
---

<objective>
Build the Walking Skeleton: one real path from a hand-authored data file, through a Zod schema,
through a repository function, into a rendered page — and one real outbound destination built
through the centralized, allowlisted integrations module and rendered as a CTA on that same page.
Then complete the integrations module for all four destinations and put the automated guards
(SEC-03 allowlist tests, ARQ-03 schema tests, ARQ-02 lint rule) behind them.

Purpose: this is the architecture Phases 2-5 inherit. Proving presentation / data / integrations
separation end-to-end now, on one thin path, is what makes a wrong turn here a one-commit fix
rather than a Phase-4 rewrite.

Output: a home page that displays real store facts and a real allowlisted iFood CTA, backed by
a validated data layer and a guarded integration layer.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-RESEARCH.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-PATTERNS.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-SKELETON.md
@.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-01-SUMMARY.md
</context>

## Artifacts this phase produces

New symbols created by plan 01-02 (none of these exist before this plan):

| Symbol / path | Kind | Signature or shape |
|---|---|---|
| `storeInfoSchema` | Zod schema | `src/lib/schemas/store.schema.ts` |
| `StoreInfo` | type | `z.infer<typeof storeInfoSchema>` |
| `externalLinkSchema`, `externalLinksSchema` | Zod schema | `src/lib/schemas/link.schema.ts` |
| `ExternalLink`, `ExternalLinkId` | type | inferred from the link schema |
| `getStoreInfo` | function | `(): StoreInfo` — `src/lib/repositories/store-repository.ts` |
| `ALLOWED_HOSTS` | const tuple | `src/lib/integrations/allowlist.ts` |
| `assertAllowedHost` | function | `(url: string) => string` — throws on a non-allowlisted hostname |
| `buildIFoodUrl` | function | `() => string` — `src/lib/integrations/ifood.ts` |
| `buildWhatsAppUrl` | function | `(message?: string) => string` — `src/lib/integrations/whatsapp.ts` |
| `buildInstagramUrl` | function | `() => string` — `src/lib/integrations/instagram.ts` |
| `buildMapsUrl` | function | `() => string` — `src/lib/integrations/maps.ts` |
| `storeData` | const | `src/data/store.ts` (default export) |
| `externalLinks` | const | `src/data/links.ts` (default export) |
| `src/app/page.tsx` | route | home route (replaces the plan 01-01 scaffold placeholder) |

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a visitor sees the real store and can reach iFood" — one path only</name>
  <files>src/data/store.ts, src/data/links.ts, src/lib/schemas/store.schema.ts, src/lib/schemas/link.schema.ts, src/lib/repositories/store-repository.ts, src/lib/integrations/allowlist.ts, src/lib/integrations/ifood.ts, src/app/page.tsx</files>
  <read_first>
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-PATTERNS.md` — the verbatim code templates for `store.schema.ts`, `store-repository.ts`, `allowlist.ts`, and `whatsapp.ts` (there is no in-repo analog; these excerpts ARE the template)
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-RESEARCH.md` — `### Pattern 1`, `### Pattern 2`, `### Anti-Patterns to Avoid`, and `## Open Questions` item 3 (the placeholder-value rule)
    - `.planning/PROJECT.md` — `## Business Context` and `## Context` (the confirmed address, neighbourhood, city, modalities, Instagram handle, and the explicit statement that the operating hours found in `img/` are 2.5-4.8 years old and must NOT be treated as current)
    - `src/app/page.tsx` — the scaffold placeholder written by plan 01-01, which this task replaces
  </read_first>
  <action>
    Wire ONE entry point through every layer of this project's architecture, end to end. No other
    call sites, no batching, no second page. Production-quality on the single path: the code
    written here is kept, not thrown away.

    **Data layer.** Create `src/data/store.ts` exporting a default object with the store facts
    confirmed in PROJECT.md and nothing else: name `It's Garlic`; address `Rua José Bonifácio, 747`;
    neighborhood `Mercado da Torre`; city `Recife`; state `PE`; modalities listing balcão, delivery,
    and take away. Its `hours` object carries `provisional` set to boolean true, a
    `pendingConfirmation` sentence in Portuguese explaining that the only references found are
    divergent Instagram stories from 2.5-4.8 years ago and that the real schedule awaits client
    confirmation, and an empty `schedule` array. Do NOT copy either of the two stale schedules from
    PROJECT.md into the data file — they are format references only, and LOCAL-03 in Phase 2 owns
    the labelled provisional display.

    Create `src/data/links.ts` exporting a default array of four destination records keyed
    `ifood`, `whatsapp`, `instagram`, `maps`. Instagram resolves to the real confirmed profile
    `itsgarlicrecife` and carries `confirmed` true. Maps resolves to a Google Maps search URL built
    from the confirmed address string and carries `confirmed` true. iFood and WhatsApp are both
    unconfirmed per REQUIREMENTS.md bloco 5: each carries `confirmed` false, a `pendingConfirmation`
    sentence, and a URL whose path segment is the literal token
    `PLACEHOLDER_PENDING_CLIENT_CONFIRMATION`. The hostname must still be a real official host
    (`www.ifood.com.br`, `wa.me`) so the allowlist guard exercises correctly, but the path must be
    obviously invalid so it fails any real click-test loudly — never a plausible-looking guessed
    store slug or phone number.

    **Schema layer.** Create `src/lib/schemas/store.schema.ts` following the PATTERNS.md template
    exactly: a Zod v4 `z.object` with `name`, `address`, `neighborhood`, `city`, and `state`; a
    `modalities` array constrained by `z.enum`; and a nested `hours` object whose `provisional`
    field is `z.literal(true)`, whose `pendingConfirmation` is a non-empty string, and whose
    `schedule` is an array of day/open/close records. The literal-true constraint is deliberate:
    once the client confirms real hours, flipping the flag is a visible schema edit rather than a
    silent data change. Export the inferred `StoreInfo` type via `z.infer`. Use Zod v4 idioms —
    shape-spread rather than `.merge()`, and `error.issues` rather than the v3 `error.errors` name.

    Create `src/lib/schemas/link.schema.ts` with `externalLinkSchema` covering `id` (a `z.enum` over
    the four destination keys), `url` (validated as a URL), `confirmed` (boolean), and an optional
    `pendingConfirmation` string. Add a `.refine` enforcing the invariant that an unconfirmed record
    must carry a non-empty `pendingConfirmation` — an unconfirmed destination with no explanation is
    exactly the state that silently ships a guessed link. Export `externalLinksSchema` as a
    `z.array` of that shape, plus the inferred `ExternalLink` and `ExternalLinkId` types.

    **Repository layer.** Create `src/lib/repositories/store-repository.ts` exporting
    `getStoreInfo()`, which imports the raw store data and returns `storeInfoSchema.parse(...)`.
    Use `.parse`, not `.safeParse` — a malformed edit must throw and fail the build (ARQ-03), not
    degrade into a rendered page with a blank address.

    **Integration layer.** Create `src/lib/integrations/allowlist.ts` from the PATTERNS.md template:
    export `ALLOWED_HOSTS` as an `as const` tuple containing the official hostnames for iFood,
    WhatsApp, Instagram, and Google Maps, and export `assertAllowedHost(url)` which parses the URL
    with the WHATWG `URL` constructor, compares `hostname` by exact equality against the tuple, and
    throws a descriptive `Error` naming the blocked hostname otherwise. Exact equality is the
    load-bearing detail — a `endsWith`/`includes` comparison would accept a look-alike host that
    merely suffixes an allowed domain.

    Create `src/lib/integrations/ifood.ts` exporting `buildIFoodUrl()`, which reads the `ifood`
    record from the link data, passes its URL through `assertAllowedHost`, and returns it. This is
    the only integration module the tracer needs; the remaining three land in Task 2.

    **Presentation layer.** Replace `src/app/page.tsx` with a Server Component (no `"use client"`,
    no client hooks, no `new Date()`) that calls `getStoreInfo()` and `buildIFoodUrl()`, renders the
    store name as the page heading, renders the street address with its neighbourhood, city, and
    state, and renders one anchor labelled "Pedir no iFood" whose href is the built URL, carrying
    `target="_blank"` and `rel="noopener noreferrer"`. Styling is deliberately minimal here — the
    brand shell lands in plan 01-06. What matters is that the data and the href both arrive through
    the seams, so use no literal address string and no literal URL anywhere in this file.
  </action>
  <verify>
    <automated>npm --prefix . run build</automated>
    <fails_when>non-zero exit, or the output contains "Failed to compile" or a Zod issue trace (which would mean the seed data does not satisfy its own schema)</fails_when>
    <automated>node -e "const fs=require('fs'),p=require('path'),root='.next/server/app';if(!fs.existsSync(root)){console.error('no prerendered app output at '+root);process.exit(1)};const files=fs.readdirSync(root,{recursive:true}).map(String).filter(f=>f.endsWith('.html'));if(files.length===0){console.error('zero prerendered HTML files - the home route did not statically render');process.exit(1)};const html=files.map(f=>fs.readFileSync(p.join(root,f),'utf8')).join('');if(!html.includes('Mercado da Torre')){console.error('store data did not reach the prerendered HTML');process.exit(1)};if(!html.includes('ifood.com.br')){console.error('allowlisted iFood href did not reach the prerendered HTML');process.exit(1)};if(!html.includes('PLACEHOLDER_PENDING_CLIENT_CONFIRMATION')){console.error('the iFood destination is not the unconfirmed placeholder - a real or guessed URL may have been substituted');process.exit(1)};console.log('tracer path OK across '+files.length+' prerendered file(s)')"</automated>
    <fails_when>non-zero exit, or stderr reports missing prerendered output, zero HTML files, absent store data, absent iFood href, or a substituted non-placeholder destination</fails_when>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('src/lib/integrations/allowlist.ts','utf8');if(!/ALLOWED_HOSTS/.test(t)||!/assertAllowedHost/.test(t)){console.error('allowlist module does not export the expected guard');process.exit(1)};const page=fs.readFileSync('src/app/page.tsx','utf8');if(!/getStoreInfo/.test(page)){console.error('page does not read store data through the repository seam');process.exit(1)};if(!/buildIFoodUrl/.test(page)){console.error('page does not build its CTA through the integrations seam');process.exit(1)};console.log('seams wired')"</automated>
    <fails_when>non-zero exit, or stderr names a missing allowlist export or a page that bypasses either seam</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/repositories/store-repository.ts` exports `getStoreInfo` and its body calls
      `storeInfoSchema.parse`.
    - `src/lib/integrations/allowlist.ts` exports `ALLOWED_HOSTS` and `assertAllowedHost`, and the
      hostname comparison is an exact-equality membership test (not `endsWith` and not `includes`
      against the URL string).
    - `src/lib/integrations/ifood.ts` exports `buildIFoodUrl` and its body calls
      `assertAllowedHost`.
    - `src/app/page.tsx` contains the identifiers `getStoreInfo` and `buildIFoodUrl`, and contains
      no string literal of the street address and no `https://` URL literal.
    - `src/app/page.tsx` contains no `"use client"` directive and no `new Date(` call.
    - `src/data/store.ts` `hours.provisional` is boolean `true`, `hours.schedule` is an empty array,
      and `hours.pendingConfirmation` is a non-empty Portuguese sentence.
    - Neither of the two stale schedules recorded in PROJECT.md appears in `src/data/store.ts`.
    - The `ifood` and `whatsapp` records in `src/data/links.ts` each have `confirmed` set to `false`,
      a non-empty `pendingConfirmation`, and a URL containing
      `PLACEHOLDER_PENDING_CLIENT_CONFIRMATION`.
    - `npm run build` exits 0 and the prerendered HTML contains `Mercado da Torre` and an
      `ifood.com.br` href.
  </acceptance_criteria>
  <done>The home page, built from a clean tree, renders the real store name and address sourced through `getStoreInfo()` and one iFood CTA whose href was produced by `buildIFoodUrl()` and cleared by `assertAllowedHost()`. The single happy path works end to end and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the integrations module and put the SEC-03 / ARQ-03 automated guards behind it</name>
  <files>src/lib/integrations/whatsapp.ts, src/lib/integrations/instagram.ts, src/lib/integrations/maps.ts, src/lib/integrations/allowlist.test.ts, src/lib/integrations/integrations.test.ts, src/lib/schemas/store.schema.test.ts, eslint.config.mjs</files>
  <read_first>
    - `src/lib/integrations/allowlist.ts` and `src/lib/integrations/ifood.ts` — written in Task 1; match their shape exactly
    - `src/lib/schemas/store.schema.ts` and `src/lib/schemas/link.schema.ts` — written in Task 1
    - `src/data/links.ts` — written in Task 1
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-PATTERNS.md` — the `allowlist.test.ts` and `whatsapp.ts` templates
    - `vitest.config.mts` — the runner config from plan 01-01 (node environment, tsconfig path alias)
  </read_first>
  <behavior>
    - `assertAllowedHost` returns the URL unchanged for each of the nine official hostnames in `ALLOWED_HOSTS`.
    - `assertAllowedHost` throws for a host that merely suffixes an allowed domain (an attacker-controlled subdomain of a look-alike registrable domain).
    - `assertAllowedHost` throws for a host that merely contains an allowed domain as a substring in a different registrable domain.
    - `assertAllowedHost` throws for a string that is not a parseable absolute URL.
    - `buildIFoodUrl`, `buildWhatsAppUrl`, `buildInstagramUrl`, and `buildMapsUrl` each return a URL whose hostname is in `ALLOWED_HOSTS`.
    - `buildWhatsAppUrl("olá")` percent-encodes the message into the query string rather than interpolating it raw.
    - `storeInfoSchema.parse` rejects an object with the address removed, and the thrown issue path names `address`.
    - `storeInfoSchema.parse` rejects an object whose `address` is an empty string.
    - `storeInfoSchema.parse` accepts `hours.schedule` as an empty array while `hours.provisional` is true.
    - `storeInfoSchema.parse` rejects `hours.provisional` set to false (the literal-true gate).
    - `externalLinkSchema.parse` rejects a record with `confirmed: false` and no `pendingConfirmation`.
  </behavior>
  <action>
    Write the tests first, watch them fail against the still-missing builders, then implement.

    Create `src/lib/integrations/whatsapp.ts`, `instagram.ts`, and `maps.ts` following the
    `ifood.ts` shape from Task 1: each reads its own record from the link data, passes the URL
    through `assertAllowedHost`, and returns it. `buildWhatsAppUrl` additionally accepts an optional
    message and appends it as a `text` query parameter, percent-encoded with `encodeURIComponent` —
    an unencoded message is how a stray `&` silently truncates the prefilled text.

    Create `src/lib/integrations/allowlist.test.ts` covering the allowlist behaviours listed above.
    The two rejection cases that matter most are the adjacency cases: a hostname that ends with an
    allowed domain but belongs to a different registrable domain, and a hostname that embeds an
    allowed domain as a substring. Both must throw. Assert on the throw, not on a falsy return.

    Create `src/lib/integrations/integrations.test.ts` asserting that each of the four builders
    returns a URL whose parsed hostname is a member of `ALLOWED_HOSTS`, and that
    `buildWhatsAppUrl` percent-encodes its message argument.

    Create `src/lib/schemas/store.schema.test.ts` covering the schema behaviours listed above,
    including the empty-`schedule` acceptance case and the `provisional: false` rejection case. Add
    one case for `externalLinkSchema` asserting the unconfirmed-requires-explanation refinement.

    Finally, add the ARQ-02 boundary rule to `eslint.config.mjs`. Read the flat config the scaffold
    wrote, then append a config object using the `no-restricted-imports` rule to forbid importing
    `@/data/*` and any relative path resolving into `src/data`, with a message pointing the author
    at the repository functions instead. Add a second, narrower config object that re-permits those
    imports for files under `src/lib/repositories/` and `src/lib/integrations/` — those two
    directories are the sanctioned seams. Confirm the rule has teeth by running the linter and
    seeing the existing files still pass; if the rule silently matches nothing, the glob is wrong.
  </action>
  <verify>
    <automated>npx vitest run src/lib/integrations/allowlist.test.ts src/lib/integrations/integrations.test.ts src/lib/schemas/store.schema.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests collected, or any assertion is reported as failed</fails_when>
    <automated>npm --prefix . test</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 3 test files</fails_when>
    <automated>node -e "const fs=require('fs');const d='src/lib/integrations';const missing=['ifood','whatsapp','instagram','maps'].filter(n=>!fs.readFileSync(d+'/'+n+'.ts','utf8').includes('assertAllowedHost'));if(missing.length){console.error('integration builders missing the allowlist guard: '+missing.join(','));process.exit(1)};const cfg=fs.readFileSync('eslint.config.mjs','utf8');if(!/no-restricted-imports/.test(cfg)){console.error('eslint.config.mjs does not carry the ARQ-02 data-import boundary rule');process.exit(1)};console.log('guards in place')"</automated>
    <fails_when>non-zero exit, or stderr names an unguarded integration builder or a missing lint rule</fails_when>
    <automated>npm --prefix . run lint</automated>
    <fails_when>non-zero exit, or ESLint reports any error-severity finding</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/integrations/whatsapp.ts`, `instagram.ts`, and `maps.ts` each export their builder and
      each body calls `assertAllowedHost`.
    - `src/lib/integrations/allowlist.test.ts` contains at least one case asserting a throw for a
      hostname that suffixes an allowed domain, and one for a hostname that embeds an allowed domain
      as a substring.
    - `src/lib/schemas/store.schema.test.ts` contains a case accepting an empty `hours.schedule` and
      a case rejecting `hours.provisional: false`.
    - `npx vitest run` over the three test files exits 0 with at least 12 passing assertions.
    - `eslint.config.mjs` contains a `no-restricted-imports` entry naming `@/data`, plus an override
      re-permitting it for `src/lib/repositories/**` and `src/lib/integrations/**`.
    - `npm run lint` exits 0 with no error-severity findings.
  </acceptance_criteria>
  <done>All four outbound destinations are buildable only through allowlist-guarded functions, the SEC-03 rejection cases and the ARQ-03 schema cases pass as automated tests, and the data-import boundary is lint-enforced.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the ARQ-03 build gate by exercising it against a deliberately malformed edit</name>
  <files>src/lib/schemas/store.schema.test.ts</files>
  <read_first>
    - `src/data/store.ts` — the seed data from Task 1
    - `src/lib/repositories/store-repository.ts` — the parse site
    - `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-VALIDATION.md` — the ARQ-03 row of the per-task verification map
  </read_first>
  <action>
    ARQ-03's promise is that a malformed data edit fails the build rather than shipping. A schema
    that is never exercised against a real violation is a schema nobody has proven has teeth. Prove
    it, without leaving a broken file behind.

    Add a test to `src/lib/schemas/store.schema.test.ts` that loads the actual shipped data module
    (not a fixture) and asserts it parses cleanly — this is the regression guard that catches a
    future hand-edit to `src/data/store.ts` at test time rather than at deploy time.

    Then demonstrate the build-time behaviour: temporarily remove the `address` field from
    `src/data/store.ts`, run the production build, and confirm it exits non-zero with a Zod issue
    naming `address`. Restore the field immediately and re-run the build to confirm it is green
    again. Record both the failing output excerpt and the restored-green result in the plan SUMMARY —
    that recorded evidence is the ARQ-03 acceptance artefact. Do not commit the malformed state.

    Leave the working tree with `src/data/store.ts` restored byte-identical to its Task 1 content.
  </action>
  <verify>
    <automated>npx vitest run src/lib/schemas/store.schema.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests collected</fails_when>
    <automated>npm --prefix . run build</automated>
    <fails_when>non-zero exit, or the output contains a Zod issue trace (which would mean the malformed edit was not restored)</fails_when>
    <automated>git diff --exit-code -- src/data/store.ts</automated>
    <fails_when>non-zero exit — the deliberately malformed edit was left in the working tree instead of being restored</fails_when>
    <human-check>
      Open the plan SUMMARY and confirm it records (a) the exact build error text produced while
      `address` was removed, showing the Zod issue path, and (b) a green build after restoration.
      If either is missing, the ARQ-03 gate was asserted rather than demonstrated.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `src/lib/schemas/store.schema.test.ts` contains a case that imports the real
      `src/data/store.ts` module and parses it with `storeInfoSchema`.
    - `git diff --stat src/data/store.ts` reports no change relative to the Task 1 commit.
    - `npm run build` exits 0 on the restored tree.
    - The plan SUMMARY quotes the non-zero build output captured while `address` was absent,
      including the Zod issue path naming `address`.
  </acceptance_criteria>
  <done>The build gate is demonstrated, not asserted: the recorded evidence shows a malformed store edit failing the build and the restored tree building green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → third-party destination | Every order/contact path leaves the site as a plain outbound anchor. The href value is the only thing standing between a visitor and a fraudulent ordering page. |
| hand-authored `src/data/*` → rendered HTML | Commercial facts (address, hours, destinations) are edited by hand, potentially by a non-developer, and rendered as authoritative. |
| build process → static output | Validation runs at build time; anything that degrades instead of throwing ships silently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering / Spoofing | `src/lib/integrations/*`, `src/data/links.ts` | high | mitigate | Single `assertAllowedHost()` chokepoint using exact hostname equality against `ALLOWED_HOSTS`; automated rejection tests for suffix and substring look-alikes (SEC-03); ESLint `no-restricted-imports` prevents a component from routing around the module (INTEGRA-04). |
| T-01-05 | Spoofing | unconfirmed iFood / WhatsApp destinations | medium | mitigate | Placeholder path token is deliberately invalid so it fails any real click-test, `confirmed: false` is machine-readable, and the link schema refuses an unconfirmed record with no explanation — three independent signals that must all be removed to ship a guess (CONT-03). |
| T-01-03 | Tampering | `src/data/store.ts` commercial facts | medium | mitigate | `storeInfoSchema.parse` at the repository boundary fails the build on a malformed edit (ARQ-03), and Task 3 demonstrates the gate against a real violation rather than assuming it. |
| T-01-10 | Information Disclosure | rendered page | low | accept | The tracer page renders only data already public (store name, street address, public profile links); no personal data, credentials, or storage APIs are touched. `localStorage`/`sessionStorage` are not used anywhere in this plan (SEC-06). |
| T-01-11 | Denial of Service | outbound destinations | low | accept | iFood availability is outside this project's control; INTEGRA-03 (a visible notice when iFood is unavailable) is Phase 2 scope and is not silently absorbed here. |
</threat_model>

<verification>
1. `npm run build` exits 0 and the prerendered HTML contains the real neighbourhood and an `ifood.com.br` href.
2. `npm test` exits 0 across at least three test files.
3. Every file in `src/lib/integrations/` that builds a URL calls `assertAllowedHost`.
4. `npm run lint` exits 0 with the `@/data/*` import boundary active.
5. The SUMMARY records a demonstrated (not asserted) ARQ-03 build failure and its restoration.
</verification>

<success_criteria>
A visitor loading the home page sees the store's real name and address and can click through to
iFood; a developer who mistypes a destination hostname or deletes a required data field finds out
at build or test time, in one place, rather than in production.
</success_criteria>

<output>
Create `.planning/phases/01-foundation-architecture-brand-identity-security-baseline/01-02-SUMMARY.md` when done.
It MUST include the captured build-failure output from Task 3 and the restored-green confirmation.
</output>
