---
context: phase
phase: 02-hero-ctas-location
task: verify-work: present_ready (gap closure planning done, execution not started)
total_tasks: n/a — this is the post-UAT gap-closure sub-flow, not a PLAN.md task list
status: in_progress
last_updated: 2026-09-13T23:15:50.809Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Treating G-02-4 (separators) as independent of G-02-5 (dead tokens) | The diagonal-wedge separator issue looks purely structural/cosmetic, but it is currently *unobservable*: every background on the live page renders browser-default white because of G-02-5's CSS token cycle. A resuming agent that "fixes" separators before/without G-02-5 will be unable to verify anything visually. | blocking | Execute 02-04-PLAN.md (G-02-5) before 02-05/02-06. The planner already sequenced this as a strict wave chain (4→5→6) — do not reorder or parallelize. |
| Assuming Test 5's focus-ring bug is scoped to focus rings | Debug agent's evidence shows the ENTIRE brand palette (backgrounds, text colors, both font families) is dead site-wide, not just the `:focus-visible` outline. | advisory | 02-04-PLAN.md already addresses the root cause (the self-referential `@theme inline` cycle + `@import` order in globals.css), not just the focus rule — don't scope a fix narrower than that plan already does. |

<current_state>
Phase 02 (hero-ctas-location) had already been implemented and UAT'd once before (see commits b4027ef, d21cbe5 — "14/14 must-haves, human_needed for end-of-phase UAT"). This session ran `/gsd-verify-work 02`, resumed the existing 02-UAT.md session (user chose to resume rather than restart), and walked all 5 human UAT checkpoints:

- Test 1 (hero brand fidelity/CLS) — **pass**
- Test 2 (CTA row fold + keyboard) — **issue**: CTAs stack instead of sitting side by side at 375/390px, and the CTA row sits outside the Hero (after the illustration), so it fails INTEGRA-01's above-the-fold criterion → **G-02-2**
- Test 3 (Location tone + chip-wrap) — **pass**
- Test 4 (brand-story voice + section separators) — **issue**: voice/content fine, but the diagonal-wedge separator only exists after the brand-story section; Location and FAQ share the same charcoal background with no separator of their own → **G-02-4**
- Test 5 (FAQ keyboard + zero-JS) — **issue**: Tab navigation works but no visible focus ring anywhere (FAQ questions, "Como chegar" link) → **G-02-5**

All 3 gaps were diagnosed by parallel `gsd-debugger` agents (sequential dispatch — a worktree base-mismatch auto-degraded parallel isolation to `none`), root causes fully written into `02-UAT.md` and into `.planning/debug/DEBUG-*.md` files, then `gsd-planner` (mode `gap_closure`) wrote three gap-closure plans, and `gsd-plan-checker` returned **VERIFICATION PASSED** on all three. Nothing about the plans needs revision. **Execution has not started.** The user ran `/gsd-pause-work` right as the plan-checker's verdict arrived.
</current_state>

<completed_work>

Completed:
- UAT session complete: 2 passed / 3 issues, committed `5febfca`
- Diagnosis complete (3 debug sessions in `.planning/debug/`), root causes written into `02-UAT.md`, committed `7cecb75` (UAT) + `400f38c` (debug files)
- Gap-closure planning complete: `02-04-PLAN.md`, `02-05-PLAN.md`, `02-06-PLAN.md` written, committed `92c09bc`
- Plan verification complete: gsd-plan-checker returned **VERIFICATION PASSED** for all 3 plans (requirement coverage, task completeness, dependency graph, key links, scope, verification strategy, security — all clean)
</completed_work>

<remaining_work>

- Present the gap-closure summary to the user (verify-work's `present_ready` step — cosmetic, the content is already known: 3/3 gaps fully addressed, plans verified)
- **Execute the gap-closure plans**, in strict order, do not parallelize:
  1. `02-04-PLAN.md` (wave 4) — fixes **G-02-5**: break the CSS custom-property self-reference cycle in `src/styles/design-tokens.css`'s `@theme inline` block + fix the `@import` order in `src/app/globals.css` so Tailwind's `@theme` layer isn't emitted unlayered-and-last. Restores the entire brand palette AND both font families (Anton/Manrope), not just the focus ring. Adds a compiled-CSS regression guard.
  2. `02-05-PLAN.md` (wave 5, depends on 02-04) — fixes **G-02-2**: amend `02-CONTEXT.md` D-01 (dated D-01a amendment — D-01 self-documents as reversible) to move the order-CTA row inside `Hero.tsx` above the illustration instead of as a sibling section in `page.tsx`; fix `CtaGroup.tsx`'s wrong `sm:` breakpoint and `OrderCta.tsx`'s label width so both buttons fit side-by-side at 343px; adds a fold-contract test (co-visibility mechanics, not source-adjacency).
  3. `02-06-PLAN.md` (wave 6, depends on 02-05) — fixes **G-02-4**: build one shared separator component, apply it at all three charcoal seams (BrandStory→Location, Location→Faq, **and the previously-unreported Faq→Footer**), sized/colored for measured contrast (Oliva, 3.74:1 — the only brand value clearing WCAG's 3:1 non-text minimum); also implements the UI-SPEC's never-implemented 48px inter-section gap.
- After execution produces `02-04-SUMMARY.md`, `02-05-SUMMARY.md`, `02-06-SUMMARY.md`: re-run `/gsd-verify-work 02`. It will reconcile gaps `G-02-2`, `G-02-4`, `G-02-5` (via the `reconcile_gaps` step, matching each plan's `gap_ids` frontmatter against its SUMMARY) to `status: resolved`, and — assuming no regressions — advance the phase to complete.
</remaining_work>

<decisions_made>

- Resumed the existing UAT session rather than restarting (user's explicit choice).
- Diagnosis ran sequentially, not in parallel: `worktree.base-check` reported the local HEAD ahead of `origin/HEAD` (nothing has been pushed this session), which auto-degrades `ISOLATION` from `harness-worktree` to `none` per the #2649 guard in `diagnose-issues.md`. This is expected GSD behavior when commits pile up locally without an intervening push — not a bug to fix.
- gsd-planner chose a strict wave chain (02-04→02-05→02-06) over parallel waves: G-02-5 blocks G-02-4 observably, and restoring real font tokens (Anton/Manrope) changes line-height/line-count math that G-02-5's fold-layout fix (in 02-05) is tuned against. 02-05 and 02-06 also both touch `src/app/page.tsx`, so they cannot share a wave regardless.
</decisions_made>

<blockers>
None currently. (The plan-checker agent that was mid-flight when `/gsd-pause-work` was invoked has already returned — VERIFICATION PASSED — so there is nothing pending to poll for.)
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-hero-ctas-location/02-UAT.md` — full UAT results + diagnosed root causes for all 3 gaps (read this first, it's the source of truth for *what* is broken and *why*)
2. `.planning/debug/DEBUG-cta-row-stacked-below-fold.md` — G-02-2 full evidence trail
3. `.planning/debug/DEBUG-focus-ring-invisible-faq-directions.md` — G-02-5 full evidence trail (the dead-palette finding)
4. `.planning/debug/DEBUG-charcoal-sections-no-separator.md` — G-02-4 full evidence trail
5. `.planning/phases/02-hero-ctas-location/02-04-PLAN.md`, `02-05-PLAN.md`, `02-06-PLAN.md` — the plans to execute, in this order

## Infrastructure State
- No dev server or watcher was left running by this session.
- Local git HEAD is ahead of `origin/HEAD` — nothing has been pushed. This is why isolation degraded to sequential during diagnosis; it will likely do the same for any future parallel dispatch until a push happens. Not blocking, just expect sequential (not parallel) subagent execution until then.
- Untracked files present (`.planning/research/.cache/*.json`, 5 files) — pre-existing research cache artifacts, unrelated to phase 02 work, safe to ignore or gitignore.

<context>
The session's real discovery was that the reported focus-ring bug (Test 5) is a symptom of a much bigger, silent defect: the entire brand color palette (and both fonts) has been dead site-wide since commit `11014a9` (phase 01-05, the token-publishing commit) — a CSS custom-property self-reference cycle in `@theme inline` plus an `@import` ordering mistake. It surfaced only now because phase 02 is the first phase to UAT keyboard navigation. This also fully explains why the separator gap (Test 4) looks the way it does: nothing colored is rendering correctly, so "no separator" and "one separator" currently look almost identical. The planner already built this dependency into the wave ordering — trust it, don't try to parallelize or reorder 02-04/05/06.
</context>

<next_action>
Start with: run `/gsd-execute-phase 02` (equivalently `/gsd-execute-phase 02 --gaps-only`) to execute `02-04-PLAN.md` → `02-05-PLAN.md` → `02-06-PLAN.md` in that strict order. Do not re-run diagnosis or planning — both are done and verified. Once all three SUMMARY.md files exist, run `/gsd-verify-work 02` to reconcile the gaps and close out the phase.
</next_action>
