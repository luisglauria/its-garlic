// Task 1 placeholder — src/app/layout.tsx already renders the single <header> landmark and
// composes this component inside it; Task 2 replaces this body with the brand mark render.
// This component only returns the header's inner content, never a second landmark tag.
export function Header() {
  return null;
}
