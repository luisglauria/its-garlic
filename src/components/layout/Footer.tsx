// Task 1 placeholder — src/app/layout.tsx already renders the single <footer> landmark and
// composes this component inside it; Task 2 replaces this body with the repository-sourced
// store identity. This component only returns the footer's inner content, never a second
// landmark tag.
export function Footer() {
  return null;
}
